/*
collection are group of objects which is represent particular elements there 
are lots of collection in dart programming
1) List: which is represent data im order and group of object
2) Set: which is contain uniqu values only 
3) Map<k,v>: map is work like dictionary which contain key and values in pair
*/